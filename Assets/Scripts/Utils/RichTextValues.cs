using System;

public struct RichTextValues {
    public string prefix;
    public string suffix;

    public RichTextValues(string prefix, string suffix) {
        this.prefix = prefix;
        this.suffix = suffix;
    }
}
